---
arxiv_id: "2401.14192"
title: "How Can Large Language Models Understand Spatial-Temporal Data?"
authors: ["Liu", "Yu", "Wang", "Ma", "Shen"]
year: 2024
date_published: "2024-01-25"
date_ingested: "2026-06-02"

approach_class: "temporal-reasoning"
problem: "LLMs can't ingest spatial-temporal graph data without a translator layer between the numeric ST signal and tokens."
approach: "STG-Tokeniser + adapter that maps spatial-temporal graph data into a form an LLM can consume, evaluated on standard ST-forecasting benchmarks."
benchmarks: ["standard spatial-temporal forecasting benchmarks"]
results:
  - "Matches dedicated ST-forecasting models using an LLM + tokeniser/adapter"
reward_shape: "supervised"
base_model: null
adapter_type: "tokeniser + adapter"

relevance: "low"
relevance_reason: "Tangential to memory but completes the temporal taxonomy — the 'feed numeric time-series to an LLM' corner. Useful only if the work ever touches sensor/ST data."
steal:
  - "Tokeniser/adapter pattern for non-text temporal modalities"
limitations:
  - "Forecasting domain; far from conversational memory"
tags: ["temporal", "reasoning-over-time", "spatial-temporal", "forecasting", "adapter"]
---

# STG-LLM — Spatial-Temporal Data for LLMs

## Quick read

A tokeniser + adapter lets an LLM consume spatial-temporal graph data and match dedicated ST-forecasting models.

## Why it matters to us

Edge of the temporal taxonomy: numeric time-series ingestion. Low direct relevance to memory, kept for completeness of the temporal map.

## Thread

reasoning-over-time (numeric/ST modality).
